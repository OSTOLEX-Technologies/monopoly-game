import { Vector2 } from "three";

// TODO
class Cell {
    constructor(public x: number, public y: number) {

    }
}

// TODO
export const getCellCenter = (cell: Cell): Vector2 => {
    return new Vector2(0, 0);
}